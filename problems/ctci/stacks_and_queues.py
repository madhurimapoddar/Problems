def use_single_array_to_implement_three_stacks():
    """"""
    pass


def stack_min(stack):
    """Return minimum from stack in constant time."""
    # maintain a second stack that stores the minimum at every stage. When a new element is
    # added to the main stack, if its less than the min, then its added to the top of the min stack.
    # When you are popping from main stack, pop from min stack if value == min
    pass


def stack_of_plates():
    # SetOfStacks should be composed of several stacks and should create a new stack when
    # the previous one exceeds capacity. SetOfStacks.push() and SetOfStacks.pop() should behave identical
    # to a single stack.
    # implement popping from a specific stack.
    pass


def queue_via_stack():
    # Implement MyQueue class which implements a queue using 2 stacks
    pass









